/**
 * Editorial “Why we chose this” copy for excursion pages.
 * Reusable across World 2.0 destinations — keep tone advisory, never catalogue-like.
 */
export type WhyWeChoseThisExcursion = {
  /** Short lead sentence under the section title */
  lead: string;
  /** Why this experience is recommended */
  whyRecommended: string;
  /** Who it suits best */
  whoItSuits: string;
  /** What makes it special */
  whatMakesItSpecial: string;
  /** Why it works for cruise timing */
  cruiseFit: string;
  /** The experience visitors walk away with */
  theExperience: string;
};
