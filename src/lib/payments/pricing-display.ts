/**
 * Display pricing helpers for the static site.
 * Authoritative amounts are recalculated on the payments Worker from the catalogue.
 */

/** Prices are catalogue-driven per product — always configured for Zakynthos. */
export const bookingPricePerGuestEur: number | null = null;
export const isBookingPriceConfigured = true;
